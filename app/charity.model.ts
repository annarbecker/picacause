export class Charity {
  constructor(public name: string, public mission: string, public image: string, public contact: string, public goal: number, public secured: number, public category: string, public hashtag: string) {}
}
